{
  "name": "deve-sub-e2e",
  "private": true,
  "description": "Playwright E2E tests for Deve Sub UI acceptance cases UI-001..010",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "@types/node": "^22.0.0"
  }
}
