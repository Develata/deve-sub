-- Migration 0001: placeholder
--
-- Real schema migrations arrive per milestone, starting with M1
-- (infrastructure and auth/users). The physical schema source of truth is
-- this migrations/ directory; see docs/data-model/core-er.md for the entity
-- model and docs/plan/13-storage.md for the SQLite configuration policy.
--
-- This placeholder exists so the migrations directory is tracked from the
-- first commit. It is a no-op.

SELECT 1;
